spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
      config:
        server-addr: http://localhost:8848
        file-extension: yaml
        ext-config: 
        - data-id: common.yml
          group: DEFAULT_GROUP
          refresh: true